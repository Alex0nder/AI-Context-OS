# NV29 (B)

## Answer

The AI features gated by tier include:

- **financial_verdict_ai** (Pro AI feature)
- AI usage limits defined in `ai-limits.js`, which set per-agent limits by tier. 

For specific tier access, refer to the `fm-ai-usage` settings and the `ai-limits.js` file.
