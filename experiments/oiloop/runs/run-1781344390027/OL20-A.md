# OL20 (A)

## Answer

The settings that control browser integration in the Oiloop project are:

- **Safari Settings**:
  - `safariEnabled`: A boolean that enables or disables automation for Safari.
  - `chromeEnabled`: A boolean that enables or disables automation for Google Chrome.

These settings are part of the `SafariSettings` structure, which can be loaded and modified in the application.
