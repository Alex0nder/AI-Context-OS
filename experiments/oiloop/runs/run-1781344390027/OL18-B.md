# OL18 (B)

## Answer

Safari tab titles and URLs are fetched using:

- **AppleScript/JXA** for querying the window and tab information. 
- The `SafariReader` entity is responsible for resolving Safari tab titles and URLs, as well as executing close scripts for duplicate tabs.
