# OL03 (A)

## Answer

The Oiloop assistant's system prompt is compiled and built in the `SystemPromptBuilder` enum. It loads the base prompt from a resource file and constructs the final prompt using various settings, facts, rules, and context information.
