# MA39 (C)

## Answer

The validation rules for `callbackUrl` when creating an inbox are as follows:

- If `callbackUrl` is provided, it must be a valid URL.
- If the `callbackUrl` is invalid, the response will include an error indicating the issue.
