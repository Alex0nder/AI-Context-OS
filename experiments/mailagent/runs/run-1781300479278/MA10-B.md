# MA10 (B)

## Answer

The required environment variables for the Worker are:

- `DATABASE_URL` (set as a secret)
- `RESEND_API_KEY` (set as a secret)
- `RESEND_WEBHOOK_SECRET` (set as a secret)
- `API_KEY` (set as a secret)
- `INBOX_DOMAIN` (set as a secret)
