# Beacon Metrics Reference

Beacon Metrics is a compact, self-contained SaaS example used to verify the
Context OS framework. The application surface is intentionally small; the
example demonstrates traceable cores, routing evaluation, drift mapping, CI,
and agent instructions.

Run from this directory after installing `context-os`:

```bash
context-os check --min-score 85
context-os maturity --min operational
```
