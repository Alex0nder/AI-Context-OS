# example-saas (CLI scaffold)

Output of:

```bash
context-os init --name example-saas --profile saas --cursor-rule
```

Use as reference or copy `context-os/` into your repo.

```bash
# from AI-Context-OS root
npm run context-os -- validate --root examples/scaffold-saas
npm run context-os -- route "Why did MRR drop?" --root examples/scaffold-saas
```
